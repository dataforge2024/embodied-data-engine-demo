# RobotDataHub 开发指南

## 项目性质

这是一个**原型项目**，用于验证架构可行性和演示核心流程。代码会被丢弃，不会进入生产环境。

## 编码原则

### 1. 简单直接，避免过度抽象

- **不要一层包一层** —— 能直接调用就直接调用，不要为了「将来可能扩展」而提前抽象
- 避免过度使用设计模式 —— 只在确实需要时才引入 Strategy、Factory 等模式
- 优先使用简单的函数和类，而不是复杂的继承体系
- 如果一个功能可以用 50 行直接实现，不要写成 200 行的「可扩展架构」

**反例（过度抽象）:**
```python
class AbstractFileProcessorFactory:
    def create_processor(self) -> AbstractFileProcessor: ...

class FileProcessorStrategy:
    def process(self, context: ProcessingContext) -> ProcessingResult: ...

# 实际上只有一种文件类型要处理
```

**正例（简单直接）:**
```python
def process_mcap_file(file_path: Path) -> dict:
    """解析 MCAP 文件并返回元数据。"""
    # 直接实现，不绕弯子
```

### 2. 测试策略

- **只测主流程** —— 确保核心路径（成功路径 + 最关键的失败路径）能跑通
- **不追求边缘覆盖** —— 「文件权限不足」「磁盘满」「采样参数边界值」这类边缘 case 可以跳过
- **不强制 80% 覆盖率** —— 原型阶段覆盖率不是目标，能演示核心功能即可

**需要测的:**
- 文件监听 → 解析 → 上传 → 回调成功（主流程）
- 文件格式错误被拦住（避免上传坏数据）
- Agent 重启后能续传（核心可靠性）

**可以不测的:**
- 采样间隔可配置
- 磁盘空间不足
- 凭据过期后的具体错误消息
- 进度节流的精确比例

### 3. 契约层例外

`contract/` 库是**唯一需要严格对待的部分**:

- 保持 80% 覆盖率要求（它是 6 个模块的共同依赖）
- 状态机、事件注册表、OpenAPI 的测试不能省（架构错位在这里暴露）
- 生成的 JSON Schema 和 TypeScript 类型必须保持同步

原因：契约层的 bug 会传播到所有下游模块，而且难以定位。业务模块的 bug 只影响自己。

## 实现优先级

1. **先跑通，再优化** —— 第一版用最简单的方式实现，能 demo 就行
2. **先本地，再真实服务** —— 优先用 LocalObjectStore 跑通逻辑，最后再接 OSS
3. **先单线程，再并发** —— 串行处理文件队列，不做并发上传

## 何时需要抽象

只在以下情况下引入抽象层:

- **已有两个实现需要切换** —— 比如 `LocalObjectStore` 和 `OSSObjectStore` 已经存在，才定义 `ObjectStore` Protocol
- **测试需要 mock** —— 外部服务调用（OSS、Platform API）需要 Protocol 以便单测时替换
- **契约明确要求** —— 状态机、事件注册表这类跨模块共享的部分必须收口

**不需要抽象的情况:**
- 「将来可能有第二种实现」—— 等真的要加时再抽
- 「这样更 OOP」—— 原型项目不追求完美设计
- 「职责更清晰」—— 如果只是为了分层而分层，保持简单即可

## 文档要求

- README 必须包含「我依赖契约的什么 / 我暴露什么 / 我参与哪几条交互」三段
- 代码注释只在非显然的地方写（不要为每个函数都写一遍类型签名的复述）
- 复杂决策（比如为什么选 HTTP 下发凭据而不是 WebSocket）在 `openspec/changes/*/design.md` 里记录，代码里不重复

## 遇到两难时的判断标准

- **简单 vs 正确** → 选正确（状态机、幂等性、checksum 校验不能省）
- **简单 vs 完备** → 选简单（边缘 case 可以不处理）
- **简单 vs 可扩展** → 选简单（等真的要扩展时再重构）
